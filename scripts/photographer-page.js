// Photographer page body is now rendered statically by
// scripts/generate_photographer_pages.py using data from
// data/photographer-essay-overrides.js at build time, so this
// file intentionally performs no runtime content injection.
// Kept as a harmless no-op for any cached HTML that still references it.
(function () {})();
