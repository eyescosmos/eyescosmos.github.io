// Photographer page bodies are hand-maintained HTML (photographers/*.html is
// the source of truth). The old build-time renderer
// (scripts/generate_photographer_pages.py) was deleted on 2026-09-15, so this
// file intentionally performs no runtime content injection.
// Kept as a harmless no-op for any cached HTML that still references it.
(function () {})();
